<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Usuarios · Realidades Financieras</title>
  <%- include('partials-style') %>
  <style>
    body { align-items: flex-start; }
    .card { max-width: 560px; margin: 48px auto; }
    table { width: 100%; border-collapse: collapse; margin-top: 16px; font-size: 13px; }
    th, td { text-align: left; padding: 8px 6px; border-bottom: 1px solid var(--border); }
    th { color: var(--muted); font-weight: 500; font-size: 11.5px; text-transform: uppercase; letter-spacing: .4px; }
    .pill { display: inline-block; padding: 2px 8px; border-radius: 999px; font-size: 11px; font-weight: 600; }
    .pill-on { background: rgba(47,209,150,0.12); color: var(--accent); border: 1px solid rgba(47,209,150,0.3); }
    .pill-off { background: rgba(255,107,107,0.1); color: var(--error); border: 1px solid rgba(255,107,107,0.3); }
    .pill-pending { background: rgba(255,193,7,0.1); color: #ffc107; border: 1px solid rgba(255,193,7,0.3); }
    .back-link { color: var(--muted); font-size: 13px; text-decoration: none; display: inline-block; margin-bottom: 16px; }
    .back-link:hover { color: var(--text); }
    .credentials-box {
      background: rgba(47,209,150,0.08);
      border: 1px solid rgba(47,209,150,0.3);
      border-radius: 10px;
      padding: 14px 16px;
      margin: 18px 0;
      font-size: 13px;
      line-height: 1.6;
    }
    .link-box {
      background: #0e1626;
      border: 1px solid var(--border);
      border-radius: 8px;
      padding: 10px 12px;
      font-family: 'SF Mono', Consolas, monospace;
      font-size: 12px;
      color: var(--accent);
      word-break: break-all;
      margin-top: 8px;
      user-select: all;
    }
  </style>
</head>
<body>
  <div class="card">
    <a href="/dashboard" class="back-link">&larr; Volver al dashboard</a>
    <div class="brand">
      <div class="brand-mark">RF</div>
      <div class="brand-name">Realidades Financieras<small>Administración de usuarios</small></div>
    </div>
    <h1>Invitar usuario</h1>
    <p class="subtitle">Se genera un link único de un solo uso. El usuario lo abre, crea su propia contraseña y configura su verificación en dos pasos. El link expira en 72 horas.</p>

    <form method="POST" action="/admin/users">
      <label for="email">Correo del nuevo usuario</label>
      <input type="email" id="email" name="email" required placeholder="usuario@empresa.com" />

      <% if (error) { %>
        <div class="error"><%= error %></div>
      <% } %>

      <button type="submit">Generar invitación</button>
    </form>

    <% if (inviteLink) { %>
      <div class="credentials-box">
        Invitación creada para <strong><%= inviteLink.email %></strong>. Copia este link y envíaselo por un canal directo (WhatsApp, etc.) — no queda guardado en ningún otro lugar:
        <div class="link-box"><%= inviteLink.url %></div>
      </div>
    <% } %>

    <h1 style="font-size:16px; margin-top:32px;">Usuarios existentes</h1>
    <table>
      <thead>
        <tr><th>Correo</th><th>Estado</th><th>Rol</th></tr>
      </thead>
      <tbody>
        <% users.forEach(u => { %>
          <tr>
            <td><%= u.email %></td>
            <td>
              <% if (u.totp_enabled) { %>
                <span class="pill pill-on">Activo</span>
              <% } else if (u.invite_token) { %>
                <span class="pill pill-pending">Invitación pendiente</span>
              <% } else { %>
                <span class="pill pill-off">2FA sin configurar</span>
              <% } %>
            </td>
            <td><%= u.is_admin ? 'Admin' : 'Usuario' %></td>
          </tr>
        <% }) %>
      </tbody>
    </table>
  </div>
</body>
</html>
