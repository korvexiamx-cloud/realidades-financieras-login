<style>
  :root {
    --bg: #0b1220;
    --card: #131b2e;
    --border: #223047;
    --text: #e6ebf2;
    --muted: #8a99b3;
    --accent: #2fd196;
    --accent-dark: #17a878;
    --error: #ff6b6b;
  }
  * { box-sizing: border-box; }
  body {
    margin: 0;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    background: radial-gradient(circle at top, #10192b, var(--bg));
    font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
    color: var(--text);
    padding: 24px;
  }
  .card {
    width: 100%;
    max-width: 380px;
    background: var(--card);
    border: 1px solid var(--border);
    border-radius: 16px;
    padding: 36px 32px;
    box-shadow: 0 20px 60px rgba(0,0,0,0.4);
  }
  .brand {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 28px;
  }
  .brand-mark {
    width: 36px; height: 36px;
    border-radius: 9px;
    background: linear-gradient(135deg, var(--accent), var(--accent-dark));
    display: flex; align-items: center; justify-content: center;
    font-weight: 700; color: #06251b; font-size: 16px;
  }
  .brand-name { font-weight: 600; font-size: 15px; letter-spacing: 0.2px; }
  .brand-name small { display: block; color: var(--muted); font-weight: 400; font-size: 11px; }
  h1 { font-size: 20px; margin: 0 0 6px; }
  p.subtitle { color: var(--muted); font-size: 13.5px; margin: 0 0 24px; line-height: 1.5; }
  label { display: block; font-size: 12.5px; color: var(--muted); margin-bottom: 6px; margin-top: 16px; }
  input[type=email], input[type=password], input[type=text] {
    width: 100%;
    padding: 11px 12px;
    background: #0e1626;
    border: 1px solid var(--border);
    border-radius: 9px;
    color: var(--text);
    font-size: 14px;
    outline: none;
    transition: border-color .15s;
  }
  input:focus { border-color: var(--accent); }
  input.otp-input {
    letter-spacing: 8px;
    font-size: 20px;
    text-align: center;
    font-variant-numeric: tabular-nums;
  }
  button {
    width: 100%;
    margin-top: 24px;
    padding: 12px;
    background: var(--accent);
    color: #06251b;
    border: none;
    border-radius: 9px;
    font-size: 14.5px;
    font-weight: 600;
    cursor: pointer;
    transition: background .15s;
  }
  button:hover { background: var(--accent-dark); color: #fff; }
  .error {
    background: rgba(255,107,107,0.1);
    border: 1px solid rgba(255,107,107,0.3);
    color: var(--error);
    padding: 10px 12px;
    border-radius: 8px;
    font-size: 13px;
    margin-top: 16px;
  }
  .hint { color: var(--muted); font-size: 12px; margin-top: 18px; line-height: 1.5; text-align: center; }
  .qr-wrap {
    background: #fff;
    padding: 14px;
    border-radius: 12px;
    display: flex;
    justify-content: center;
    margin: 20px 0;
  }
  .qr-wrap img { width: 180px; height: 180px; display: block; }
  .secret-box {
    background: #0e1626;
    border: 1px solid var(--border);
    border-radius: 8px;
    padding: 10px 12px;
    font-family: 'SF Mono', Consolas, monospace;
    font-size: 12.5px;
    color: var(--accent);
    word-break: break-all;
    text-align: center;
    margin-bottom: 4px;
  }
  .badge {
    display: inline-block;
    background: rgba(47,209,150,0.12);
    color: var(--accent);
    border: 1px solid rgba(47,209,150,0.3);
    padding: 3px 9px;
    border-radius: 999px;
    font-size: 11px;
    font-weight: 600;
    letter-spacing: .3px;
  }
  .demo-note {
    margin-top: 22px;
    padding: 10px 12px;
    border-radius: 8px;
    background: rgba(138,153,179,0.08);
    border: 1px dashed var(--border);
    color: var(--muted);
    font-size: 11.5px;
    line-height: 1.5;
  }
</style>
